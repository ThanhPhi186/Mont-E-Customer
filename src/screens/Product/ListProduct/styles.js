import {getBottomSpace} from '../../../helpers/iphoneXHelper';
import {Mixin} from '../../../styles';

const styles = {
  btnGoCart: {
    alignSelf: 'center',
    position: 'absolute',
    bottom: Mixin.moderateSize(20),
  },
};

export default styles;
